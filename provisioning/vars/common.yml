---
# Variables for all hosts

user: vagrant
shell_rc: .bashrc
thrift_version: 0.9.1
pypy_version: 2.3.1
tig_version: 2.0.2

ccache_directory: /var/cache/ccache

# Monit configuration
monit_port: 2812

poco_version: 1.4.7-all

local_bin:
  PATH: "{{ lookup('env', 'PATH') }}:/usr/local/bin"
